# Samples

建议在一个空 UE 工程中安装 CesiumForUnreal 后测试：

1. 启用 GeoEarth 插件。
2. 创建包含 CesiumGeoreference 的关卡。
3. 使用 AGeoCameraController 作为默认 Pawn。
4. 使用合成 GeoJSON 或本地测试瓦片验证 GeoJSON / VectorTile 模块。

本仓不包含 sqlite 缓存、Binaries、DerivedDataCache、商业美术或 Cesium 插件本体。
