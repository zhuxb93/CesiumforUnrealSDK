# 相机控制器

## 解决什么问题
三维地球相机需要处理 ECEF 中心点、距离、旋转、俯仰和跟随目标，普通 Pawn 控制很难直接复用。

## 实现思路
以 APawn 管理输入和相机姿态，把 FlyToLocation、ZoomTo、RotateTo、TiltTo、FollowTo 暴露给蓝图。

## 基本用法
把 AGeoCameraController 设为默认 Pawn，配置 DefaultInput.ini 后在蓝图或 C++ 中调用运镜 API。

## 依赖
CesiumForUnreal / CesiumRuntime。
